MONGODB_URI=mongodb://localhost:27017/easylease
PORT=4000
NODE_ENV=development

# Add other env variables used by the API here, but do NOT include secrets in real commits.
# For production, store secrets in GitHub Secrets, not in repo files.
