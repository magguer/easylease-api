MONGODB_URI=mongodb://localhost:27017/rentalist
PORT=4000
NODE_ENV=development

# Add other env variables used by the API here, but do NOT include secrets in real commits.
# For production, store secrets in GitHub Secrets, not in repo files.PORT=4000
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/rentalist?retryWrites=true&w=majority
NODE_ENV=development
